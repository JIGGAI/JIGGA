# JIGGA Workflows

> Workflows are **built and usable today** — authoring, `jigga workflow plan` /
> `run`, supervisor scheduling, approval gating, and workflow inference
> (`suggest` / `apply`) all work and are covered by tests. For how to use them,
> see [`WORKFLOWS_GUIDE.md`](WORKFLOWS_GUIDE.md) (the source of truth for current
> behavior). This file is the **design & rationale**, and it also sketches a few
> **planned** extensions — see "Built today vs. planned" below.

## Built today vs. planned

**Built and tested:** declarative workflow files; ordered (linear) steps with
named-output chaining; `agent`/`action`/`input`/`output`/`approval`/`optional`
fields; model-backed steps (`draft_with_model`); `trigger.schedule` firing via
the supervisor; per-step approval gating (explicit or risk-based); memory writes;
and **workflow inference** (`jigga workflow suggest` / `apply`).

**Planned (described below but not yet implemented):** event triggers like
`calendar_event_upcoming`/offsets (only `trigger.schedule` fires today),
`on_fail` step handling (the field parses but isn't enforced yet), and
conditional/branching steps (today's runner is a linear pipeline — branch
*across agents* with team handoffs instead).

## Definition

A JIGGA workflow is a reusable playbook or standard operating procedure that agents can invoke for repeatable work.

Workflows are not a mandatory central engine. They are declarative procedures used by agents, teams, schedules, and users.

## Why Workflows Matter

Without workflows, agents can still make things. But repeatable work becomes inconsistent, hard to scale, and hard to inspect.

Workflows allow users to define repeatable processes such as:

- social media content development and syndication
- morning day summaries
- meeting reminders
- inbox triage
- research briefs
- software feature delivery
- customer follow-up

## Workflow Invocation Sources

A workflow can be invoked by:

- a user
- an agent
- a team
- a schedule
- an external event
- another workflow step

## Workflow Shape

```yaml
id: social_content_syndication
name: Social Content Syndication
purpose: Turn one source idea into platform-specific content.
inputs:
  source_material:
    type: file_or_text
    required: true
steps:
  - id: extract_core_message
    agent: strategist
    action: summarize_source
  - id: draft_linkedin
    agent: linkedin_writer
    action: draft_post
  - id: draft_x_thread
    agent: x_writer
    action: draft_thread
  - id: review
    agent: editor
    action: review_brand_voice
  - id: prepare_distribution
    agent: publisher
    action: prepare_schedule
    approval: required
outputs:
  - linkedin_post.md
  - x_thread.md
  - newsletter_blurb.md
memory:
  write_summary: true
permissions:
  required:
    - filesystem.read
    - filesystem.write
    - notifications.send
```

## Workflow Concepts

### Inputs

Required data needed to run the workflow.

### Steps

Ordered or conditional actions.

### Actions

Atomic operations performed by agents or tools.

### Outputs

Artifacts generated by the workflow.

### Gates

Approval or validation requirements.

### Failure Handling

What happens if a step fails.

### Memory Writes

What gets persisted after workflow completion.

## Approval Rules

Agents may draft or suggest workflows.

Agents should not silently activate autonomous recurring workflows.

Recommended flow:

1. Detect pattern.
2. Draft workflow.
3. Show plan/diff.
4. User approves.
5. Workflow becomes active.

## Workflow Inference

Workflow inference is a key differentiator.

The system should notice repeated behavior and suggest workflows.

Example pattern:

- User repeatedly asks for calendar and email summary every morning.
- Agent detects recurring time and action pattern.
- Agent proposes `morning_day_summary` workflow.

Suggested workflow example:

```yaml
id: morning_day_summary
name: Morning Day Summary
trigger:
  schedule: "weekday 7:30am"
steps:
  - id: check_calendar
    action: calendar.list_events
    input:
      range: today
  - id: check_email
    action: email.search
    input:
      filters: [important, unread, today]
  - id: summarize
    agent: daily_briefing_agent
    action: summarize_day
  - id: notify
    action: notifications.send
approval:
  required_to_activate: true
```

## Meeting Reminder Workflow

```yaml
id: meeting_reminders
name: Meeting Reminders
purpose: Notify the user before each meeting.
trigger:
  calendar_event_upcoming:
    offsets:
      - 30m
      - 5m
steps:
  - id: load_event
    action: calendar.get_event
  - id: gather_prep_context
    agent: meeting_prep_agent
    action: summarize_relevant_context
    optional: true
  - id: notify
    action: notifications.send
    input:
      include:
        - title
        - time
        - location_or_link
        - attendees
        - prep_notes
```

## Social Content Workflow

```yaml
id: social_content_syndication
name: Social Content Syndication
purpose: Convert one idea or source asset into platform-specific posts.
steps:
  - id: extract_core_message
    agent: content_strategist
    action: extract_core_message
  - id: draft_linkedin
    agent: linkedin_writer
    action: draft_linkedin_post
  - id: draft_x
    agent: x_writer
    action: draft_thread
  - id: draft_newsletter
    agent: newsletter_writer
    action: draft_blurb
  - id: editorial_review
    agent: editor
    action: review_tone_and_claims
  - id: prepare_schedule
    agent: publisher
    action: prepare_distribution
    approval: required
```

## Workflow States

- draft
- suggested
- planned
- approved
- active
- paused
- deprecated
- archived

## Workflow Planning

Before activation, the system should display:

- what the workflow does
- when it runs
- which agents it wakes
- which tools it uses
- what memory it reads/writes
- which permissions it requires
- what approvals are needed

Example:

```text
Workflow: morning_day_summary
Will run: weekdays at 7:30am
Will read: calendar, important unread email
Will write: daily summary memory
Will notify: user
Requires: calendar.read, email.read, notifications.send
Approval required: yes
```
